# VARAi Connected Apps Environment Configuration
# SECURITY NOTICE: This file contains environment variable placeholders only.
# All sensitive values must be managed through Google Cloud Secret Manager.

# Stripe Configuration - Retrieved from Google Cloud Secrets
# Secret names: stripe-publishable-key, stripe-secret-key, stripe-webhook-secret
STRIPE_PUBLISHABLE_KEY=${STRIPE_PUBLISHABLE_KEY}
STRIPE_SECRET_KEY=${STRIPE_SECRET_KEY}
STRIPE_WEBHOOK_SECRET=${STRIPE_WEBHOOK_SECRET}

# Stripe Product IDs - Retrieved from Google Cloud Secrets
# Secret names: stripe-starter-product-id, stripe-starter-price-id, etc.
STRIPE_STARTER_PRODUCT_ID=${STRIPE_STARTER_PRODUCT_ID}
STRIPE_STARTER_PRICE_ID=${STRIPE_STARTER_PRICE_ID}
STRIPE_PROFESSIONAL_PRODUCT_ID=${STRIPE_PROFESSIONAL_PRODUCT_ID}
STRIPE_PROFESSIONAL_PRICE_ID=${STRIPE_PROFESSIONAL_PRICE_ID}
STRIPE_ENTERPRISE_PRODUCT_ID=${STRIPE_ENTERPRISE_PRODUCT_ID}
STRIPE_ENTERPRISE_PRICE_ID=${STRIPE_ENTERPRISE_PRICE_ID}

# One-time Token Package Price IDs - Retrieved from Google Cloud Secrets
STRIPE_TOKENS_1K_PRICE_ID=${STRIPE_TOKENS_1K_PRICE_ID}
STRIPE_TOKENS_5K_PRICE_ID=${STRIPE_TOKENS_5K_PRICE_ID}
STRIPE_TOKENS_10K_PRICE_ID=${STRIPE_TOKENS_10K_PRICE_ID}

# Database Configuration - Retrieved from Google Cloud Secrets
# Secret name: database-url
DATABASE_URL=${DATABASE_URL}

# JWT Secret for authentication - Retrieved from Google Cloud Secrets
# Secret name: jwt-secret
JWT_SECRET=${JWT_SECRET}

# API Configuration
API_BASE_URL=https://commerce-studio-website-ddtojwjn7a-uc.a.run.app
WEBHOOK_URL=https://commerce-studio-website-ddtojwjn7a-uc.a.run.app/api/stripe/webhook

# Email Configuration - Retrieved from Google Cloud Secrets
# Secret names: smtp-host, smtp-port, smtp-user, smtp-pass, from-email
SMTP_HOST=${SMTP_HOST}
SMTP_PORT=${SMTP_PORT}
SMTP_USER=${SMTP_USER}
SMTP_PASS=${SMTP_PASS}
FROM_EMAIL=${FROM_EMAIL}

# Environment
NODE_ENV=production